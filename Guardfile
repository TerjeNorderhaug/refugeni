# This is an example with all options that you can specify for guard-process

guard 'process', :name => 'server', :command => 'node resources/js/server.js', :stop_signal => "KILL"  do
  watch('resources/js/server.js')
end

